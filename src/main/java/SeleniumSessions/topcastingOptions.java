package SeleniumSessions;

import org.openqa.selenium.SearchContext;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chromium.ChromiumDriver;
import org.openqa.selenium.edge.EdgeDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.remote.RemoteWebDriver;

public class topcastingOptions {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		
		//1. valid.
		
		//ChromeDriver driver = new ChromeDriver();
		
		//FirefoxDriver diver = new FirefoxDriver();
		
		//EdgeDriver driver = new EdgeDriver();
		
		//2.valid n recommended
		//WebDriver driver = new ChromeDriver();
		//WebDriver driver = new FirefoxDriver();
		
		//3.valid but not recoomended
		//SearchContext driver = new ChromeDriver();
		//SearchContext driver = new FirefoxDriver();
		
		//4.valid - recommended
		//RemoteWebDriver driver = new ChromeDriver();
		//RemoteWebDriver driver = new FirefoxDriver();
		//RemoteWebDriver driver = new EdgeDriver();
		
		//5.valid but only or chrome n edge driver
		//ChromiumDriver driver = new ChromeDriver();
		//EdgeDriver driver = new EdgeDriver();
		
		//6.recommended other machine, cloud - LT ,GRID,BrowserStack,AWS
		//WebDriver driver = new RemoteWebDriver(remoteAddress , capabilities);
		
		//7 VALID - NOT RECOMMENED
		//SearchContext driver = new RemoteWebDriver(remoteAddress , capabilities);
		
		//8 invalid.
		//RemoteWebDriver driver = new ChromiumDriver();
		
		
		
		
		

	}

}
